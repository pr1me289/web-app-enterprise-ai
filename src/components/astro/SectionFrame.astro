---
interface Props {
  id?: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="border-t border-slate-200 py-12">
  <h2 class="text-2xl font-semibold">{title}</h2>
  <div class="mt-4 prose">
    <slot />
  </div>
</section>
